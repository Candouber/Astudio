# AStudio does not require environment variables for normal local startup.
# Prefer configuring models in the settings UI or data/config.yaml.

# Optional overrides for local experiments:
# ASTUDIO_SERVER_PORT=8000
# ASTUDIO_DB_POOL_SIZE=6
# WEB_SEARCH_PROXY=http://127.0.0.1:7890
